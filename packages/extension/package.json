{
  "name": "md-requirements-editor",
  "displayName": "Markdown Requirements Editor",
  "description": "Typora-style editable preview for markdown requirements documents — review comments, traceability, and quality checks",
  "version": "0.4.2",
  "type": "commonjs",
  "publisher": "akchattapayaksh",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "mdreq.openDashboard",
        "title": "Requirements: Open Dashboard"
      },
      {
        "command": "mdreq.exportReviewCsv",
        "title": "Requirements: Export Review Comments as CSV"
      },
      {
        "command": "mdreq.exportTraceabilityCsv",
        "title": "Requirements: Export Traceability as CSV"
      },
      {
        "command": "mdreq.importReview",
        "title": "Requirements: Import Review Comments (JSON)…"
      },
      {
        "command": "mdreq.importTraceability",
        "title": "Requirements: Import Traceability (JSON)…"
      },
      {
        "command": "mdreq.saveReviewJsonAs",
        "title": "Requirements: Save Review Comments As (JSON)…"
      },
      {
        "command": "mdreq.saveTraceabilityJsonAs",
        "title": "Requirements: Save Traceability As (JSON)…"
      }
    ],
    "configuration": {
      "title": "Markdown Requirements Editor",
      "properties": {
        "mdreq.requirementPattern": {
          "type": "string",
          "default": "REQ_001",
          "markdownDescription": "Example requirement ID (simple mode). Headings whose text starts with an ID of this shape get review/traceability badges — e.g. `REQ_001`, `SRS-0001`. Ignored when `#mdreq.requirementPatternRegex#` is set. Leave both empty to disable requirement detection."
        },
        "mdreq.requirementPatternRegex": {
          "type": "string",
          "default": "",
          "markdownDescription": "Advanced: a regular expression for requirement IDs; takes precedence over the simple pattern. It must match at the START of the heading text and capture the full ID in group 1 (or a named group `id`). Example for `FEAT_<Feature>_001` IDs with a variable feature name: `(FEAT_[A-Za-z0-9]+_\\d{3})`."
        },
        "mdreq.requirementPatternRegexFlags": {
          "type": "string",
          "default": "",
          "markdownDescription": "Flags for `#mdreq.requirementPatternRegex#`, e.g. `i` for case-insensitive matching."
        },
        "mdreq.newlineBehavior": {
          "type": "string",
          "enum": [
            "line",
            "paragraph"
          ],
          "default": "line",
          "markdownDescription": "What Enter does in a paragraph. `line` (default): one newline in the file; press Enter again on the empty line for a paragraph break (blank line). `paragraph`: classic markdown — Enter always starts a new paragraph; use Shift+Enter for a line break."
        },
        "mdreq.variantVocabulary": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "markdownDescription": "Allowed values for the optional `[Variant]` bracket on requirement headings (e.g. `FEAT_login_001 [*Draft*] [V2]`). When set, the variant chip becomes a dropdown and unknown variants are flagged by the quality checks. Leave empty to allow free-text variants."
        }
      }
    },
    "customEditors": [
      {
        "viewType": "mdreq.editor",
        "displayName": "Requirements Editor",
        "selector": [
          {
            "filenamePattern": "*.md"
          }
        ],
        "priority": "option"
      }
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.mts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "MIT",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/akchat0311/md-requirements-vscode"
  },
  "bugs": {
    "url": "https://github.com/akchat0311/md-requirements-vscode/issues"
  },
  "homepage": "https://github.com/akchat0311/md-requirements-vscode#readme",
  "keywords": [
    "markdown",
    "requirements",
    "wysiwyg",
    "editor",
    "review",
    "traceability",
    "typora",
    "specification"
  ],
  "galleryBanner": {
    "color": "#1b1d22",
    "theme": "dark"
  }
}
