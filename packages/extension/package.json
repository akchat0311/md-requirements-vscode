{
  "name": "md-requirements-editor",
  "displayName": "Markdown Requirements Editor",
  "description": "Typora-style editable preview for markdown requirements documents — review comments, traceability, and quality checks",
  "version": "0.1.7",
  "private": true,
  "type": "commonjs",
  "publisher": "akchat",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "configuration": {
      "title": "Markdown Requirements Editor",
      "properties": {
        "mdreq.requirementPattern": {
          "type": "string",
          "default": "REQ_001",
          "markdownDescription": "Example requirement ID (simple mode). Headings whose text starts with an ID of this shape get review/traceability badges — e.g. `REQ_001`, `SRS-0001`. Leave empty to disable requirement detection."
        }
      }
    },
    "customEditors": [
      {
        "viewType": "mdreq.editor",
        "displayName": "Requirements Editor",
        "selector": [
          {
            "filenamePattern": "*.md"
          }
        ],
        "priority": "option"
      }
    ]
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.mts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
