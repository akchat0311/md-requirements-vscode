/**
 * Export stubs — function signatures are ready, implementations pending.
 * Menu items are present in the UI but disabled with "Coming soon" tooltip.
 */

export async function exportDocx(_markdown: string, _fileName: string): Promise<void> {
  throw new Error("DOCX export not yet implemented");
}

export async function exportPdf(_markdown: string, _fileName: string): Promise<void> {
  throw new Error("PDF export not yet implemented");
}
